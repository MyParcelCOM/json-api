version: '3'

services:
  php:
    build:
      context: ./docker
      dockerfile: Dockerfile
    image: myparcelcom/json-api:v1-dev
    working_dir: /opt/json-api
    volumes:
      - .:/opt/json-api
      - composercache:/tmp
    user: ${CURRENT_UID}

volumes:
  composercache:
    driver: local
